@attribute [Authorize]
@page "/Account/Customer/{CID}/Settings"
@using Microsoft.AspNetCore.Components.Server.ProtectedBrowserStorage
@inject ProtectedLocalStorage ProtectedLocalStorage

<PageTitle>Customer [@CID] Settings | Merchanter</PageTitle>
@* 
<NotificationsComponent CID="@CID" /> *@

@code {
    [Parameter]
    public string? CID { get; set; }
}
